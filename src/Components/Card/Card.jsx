import React from "react";

import ScrollToBottom from "react-scroll-to-bottom";

const ROOT_CSS = {
  height: 400,
  width: 400,
};

const Card = () => {
  return <ScrollToBottom className="scroll"></ScrollToBottom>;
};

export default Card;
